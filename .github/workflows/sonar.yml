name: Sonar analysis

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:
  repository_dispatch:
    types: [trigger-eks-deployment]

jobs:
  build:
    name: teste
    runs-on: ubuntu-latest
    steps:
      - name: HelloWorld
        run: echo "Hello"
